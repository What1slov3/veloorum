import { LSStructure } from '@customTypes/LSA.types';

const LSSDefault: LSStructure = {
  unsentMessages: {},
  sendingErrorMessages: {},
  settings: {
    fontSize: 14,
    messageGroupGap: 4,
  },
};

export default LSSDefault;
